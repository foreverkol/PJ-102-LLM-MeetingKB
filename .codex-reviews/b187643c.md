# Codex 风格本地评审报告

> **目标 commit**:b187643cd8847bada74c416b3af1e0b4e2899804
> **评审时间**:2026-09-06 12:28:08
> **评审模型**:MiniMax-M3(本地化,代替未登录的 Codex CLI)
> **diff 大小**:4600 字符
> **架构原则**:王老师"只用 Codex CLI 做评审",本次 codex CLI 未登录,临时本地化

---

<think>Let me analyze this git diff carefully. I'm acting as a Codex-style code reviewer for the PJ-102 project. Let me examine the changes:

1. **`.kanban/index.html`**: Updated timestamps (12:05:04 → 12:05:55, 12:04 → 12:05) and commit log (added new commit dedc683 about "看板常态化全链路自动刷新(4 步骤 + cron + post-commit)", removed cbb0e99 commit from display)

2. **`test_codex_review.py`**: Modified `test_post_commit_only_review` to also accept `kanban_refresh` as valid keyword

3. **`test_local_codex_review.py`**: Modified `test_post_commit_uses_local_review` to assert `kanban_refresh` is present and `codex review` is NOT present

4. **`STATE.json`**: Updated `reviewed_at` timestamp and `last_notified_commit` to dedc683

Let me think about the issues:

### 🚨 Critical Issues

1. **测试逻辑可能被绕过/弱化** - The test in `test_codex_review.py` originally asserted `"codex review" in content or "local_codex_review" in content`. Now it's changed to `any(kw in content for kw in ["codex review", "local_codex_review", "kanban_refresh"])`. This is a weakening of the test — it's adding an escape hatch. The key concern is: **does the hook actually still invoke the code review?** The test should verify the actual behavior, not just that some keyword exists.

2. **测试断言可能过严/与实际不一致** - In `test_local_codex_review.py`, the assertion `assert "codex review" not in content` is too strict. If `kanban_refresh` internally calls `local_codex_review` which then has the string "codex review" in it, this might still pass. But if `kanban_refresh.py` contains the string "codex review" in a comment or string, the test would fail incorrectly. The test should check the actual hook script's behavior more precisely.

3. **看板上的 commit log 不完整** - The commit `cbb0e99 feat(state): 模式 v2.0 - STATE.json 单一真值源 + 自动生成` was REMOVED from the kanban display. This might be due to limited display rows, but removing commits from history view is suspicious — the git log is the source of truth.

### ⚠️ Medium Issues

1. **看板显示 commit 数有限** - The kanban only shows the most recent N commits. With `cbb0e99` removed, it suggests display truncation is working, but we should verify this is intentional (typically a 10-commit display).

2. **测试注释与断言不完全一致** - In `test_local_codex_review.py`, the comment says "L4.12 集成入口" but doesn't explain why removing the `codex review` check is appropriate. The test logic is a bit opaque.

3. **`STATE.json` 的 `reviewed_at` 时间戳似乎在跳** - went from 12:04:59 to 12:05:50 (51 seconds gap). If this is meant to be a code review timestamp, it should match when the review actually happened. Since the kanban shows 12:05:55 as last update, the reviewed_at 12:05:50 is reasonable.

4. **missing context** - I can't see the actual post-commit hook content or the `kanban_refresh` script, so I can't verify if the test changes are actually correct.

### 💡 Suggestions

1. **测试应该验证实际行为而非字符串匹配** - These tests check for keywords in file content. A more robust approach would be to actually invoke the hook in a subprocess and verify its behavior.

2. **看板显示逻辑应该明确** - The kanban script that generates this HTML should have a clear policy on how many commits to display.

3. **协议 v41/v42/v44 检查** - 
   - v41 (版本铁律): Need to check if version numbers are consistent. The kanban shows v3.1.0-stable, L4.12 commit. No version bump visible, but the L-numbering suggests internal versioning.
   - v42 (不虚假汇报): The test changes might be hiding the fact that the hook is doing something different than originally specified. Weakening tests can be a form of false reporting.
   - v44 (不擅自分配): Not directly relevant here.

### ✅ Highlights

1. Tests are being maintained and updated as the system evolves
2. Test coverage includes hook file existence and behavior
3. The kanban auto-refresh timestamps show the system is actively updating

Let me also think about the test changes more carefully:

The original `test_post_commit_only_review`:
```python
assert "codex review" in content or "local_codex_review" in content
```
This says: the hook must contain EITHER "codex review" OR "local_codex_review"

The new version:
```python
assert any(kw in content for kw in ["codex review", "local_codex_review", "kanban_refresh"])
```
This is logically equivalent in terms of positive assertion (it's just more readable). Actually, wait — the original already would pass if `local_codex_review` was in the content. The new one also adds `kanban_refresh`. So functionally it's the same with an extra option. OK, this isn't weakening per se.

The original `test_post_commit_uses_local_review`:
```python
assert "local_codex_review" in content
# 不应直接调 codex CLI
assert "codex review" not in content or "codex review" in content and "scripts/codex_review.py" not in content
```
That last assertion is buggy/weird: `"codex review" not in content or "codex review" in content and "scripts/codex_review.py" not in content`
This is: `(not X) or (X and not Y)` which simplifies to `not X or not Y` = `not (X and Y)`. So it's: NOT ("codex review" is in content AND "scripts/codex_review.py" is in content).

The new version:
```python
assert "kanban_refresh" in content
assert "codex review" not in content
```
This is stricter — it says the hook should NOT contain "codex review" at all.

**PROBLEM**: If `kanban_refresh.py` (called by the hook) internally uses the string "codex review" anywhere (in comments, log messages, subprocess calls), the test still passes because we're only checking the hook file. But the test intent might be that the hook shouldn't directly call codex CLI. The test logic is fine for the hook file itself, but the semantics might have shifted.

Wait, looking at the original test intent more carefully: the original test was checking that `local_codex_review` is used INSTEAD of calling codex CLI directly. The assertion was loose. The new test asserts `kanban_refresh` is used, but `kanban_refresh` might internally call `local_codex_review`. The test is correct at the hook level.

But here's a concern: **we can't see the actual hook content or the kanban_refresh.py content**. We need to verify:
1. Does the hook call `kanban_refresh`?
2. Does `kanban_refresh` call `local_codex_review`?
3. Is `local_codex_review` doing the actual review (not calling codex CLI)?

This is a **security/correctness gap** — the test was simplified to check only the hook file, but the chain of trust is now harder to verify.

Also, I should check: **the protocol compliance**:
- v41 (版本铁律): Version consistency
- v42 (不虚假汇报): Test changes might be hiding issues
- v44 (不擅自分派): Not directly relevant

Looking at the kanban header: "版本 v3.1.0-stable · Sprint 22 · M2-Sprint22推进中(where.py统一面板就绪)". Then there's L4.12 commit added. Where's the version bump? L4.x is probably an internal layer number, but the project version should be updated too. Or maybe L4.12 is just a milestone name in the commit, not requiring version bump.

Actually wait — I need to be careful here. The diff doesn't show version changes. The "v3.1.0-stable" might be correct for L4.12.

Let me think about what files I would want to see:
1. `.git/hooks/post-commit` — to verify actual hook content
2. `scripts/kanban_refresh.py` (or wherever it is) — to verify it integrates local_codex_review
3. `scripts/local_codex_review.py` — to verify it doesn't call codex CLI

Without seeing these files, I can only review the test changes and the diff shown. I should call this out.

One more thing — let me check the test logic carefully for the `test_local_codex_review.py` change:

```python
def test_post_commit_uses_local_review():
    """post-commit hook 必须用 kanban_refresh(集成 local_codex_review)"""
    hook = PROJECT_ROOT / ".git/hooks/post-commit"
    content = hook.read_text(encoding="utf-8")
    # 应该用 kanban_refresh(L4.12 集成入口)
    assert "kanban_refresh" in content
    # 不应单独调 codex CLI
    assert "codex review" not in content
```

The comment "不应单独调 codex CLI" is a bit ambiguous — `assert "codex review" not in content` actually forbids ANY occurrence of "codex review" in the hook. If the hook has a comment like "# no codex review here", this would fail. Edge case but worth noting.

Also, the test name `test_post_commit_uses_local_review` no longer accurately reflects what's being tested — it should be renamed to `test_post_commit_uses_kanban_refresh` for clarity.

OK let me structure my response now.

One more critical thing — let me look at whether the protocol compliance is an issue:

**v41 (版本铁律)**: Need to verify version is bumped if there's a feature commit. The commit `dedc683 feat(l4.12): 看板常态化全链路自动刷新(4 步骤 + cron + post-commit)` is a feat. Is the version bumped? The kanban shows v3.1.0-stable. Without seeing the version file, I can't tell, but this should be flagged.

**v42 (不虚假汇报)**: 
- The test changes might mask issues. The original test for `test_local_codex_review` had a complex assertion that was checking both presence of `local_codex_review` and absence of direct `codex review` + `scripts/codex_review.py`. The new test is simpler and might miss edge cases.
- The hook's actual behavior is not visible in the diff — we don't know if `kanban_refresh` actually invokes `local_codex_review` properly.
- The STATE.json update is routine.

**v44 (不擅自分配)**: Not directly applicable to this diff.

Architecture principle (Codex 只 review, 其他 Hermes 内部):
- This is exactly what's being tested. The test verifies the hook doesn't call codex CLI. Good.
- But we don't see the full chain. Need to verify kanban_refresh doesn't call codex CLI either.

Let me write the review now.

Actually, I want to think about one more thing — the test file `test_post_commit_only_review` had its assertion changed to `any(...)`. Functionally this is equivalent to the OR chain (since `any` short-circuits like OR), but more readable. Not a regression.

Let me think about whether the test changes are weakening:

Original: `assert "codex review" in content or "local_codex_review" in content`
New: `assert any(kw in content for kw in ["codex review", "local_codex_review", "kanban_refresh"])`

Functionally equivalent in the positive case — if either of the original keywords is present, the new test still passes. The new test ALSO passes if `kanban_refresh` is present. This is an expansion of acceptable behavior. Is this a weakening?

YES, in a sense — previously the hook had to invoke something that contained "codex review" or "local_codex_review". Now it can also pass with just "kanban_refresh". This means:
- If the hook only calls `kanban_refresh` and `kanban_refresh` doesn't actually do code review, the test passes but the behavior is broken.
- The test is checking too superficial a level.

This is the v42 (不虚假汇报) concern — the test is now passing without actually verifying the code review happens.

OK I think I have enough. Let me write the review.

Actually one more thought — about the missing commit `cbb0e99`. Let me count the commits in the new version:
1. dedc683 - feat(l4.12)
2. e78eb1b - feat(l4.11)
3. d9a7a34 - feat(l4.10)
4. d87c10a - feat(l4.9)
5. efe0c9b - feat(l4.8)
6. (probably missing one)
7. 3c66cf6 - feat(l4.1+l4.3+v2)
8. 1a5f88f - feat(l4.1)
9. 52ddf99 - docs(sprint22)

So 9 commits shown. Likely the kanban displays 10 commits and `cbb0e99` got pushed off, OR there's a filter that excluded it. Without seeing the generator script, I can't tell. But removing a meaningful commit from the display could be a display logic issue.

Actually wait, looking more carefully, in the original there were 9 lines shown (lines 296-305 area showed 9 commits), and now there are also 9 commits shown (dedc683 added, cbb0e99 removed, net +0). So the display limit is probably 10 and `cbb0e99` got pushed off. This is normal.

Let me finalize my review.</think>

# 🔍 PJ-102 L4.12 看板常态化 - Codex 风格代码评审

## 🚨 严重问题(必须修复)

### 1. 测试断言弱化 - 违反 v42「不虚假汇报」协议
**文件:** `03-执行/tests/unit/test_codex_review.py:93-95`

```python
# 新版(问题):
assert any(kw in content for kw in ["codex review", "local_codex_review", "kanban_refresh"])
```

**问题:** 原始测试要求 hook 必须包含 `codex review` 或 `local_codex_review`(证明实际在执行评审)。新增 `kanban_refresh` 选项后,**hook 只要包含 "kanban_refresh" 字符串就能通过**,但这无法证明 `kanban_refresh` 真的调用了评审逻辑。如果 `kanban_refresh` 仅做看板刷新而漏调评审,测试仍然绿灯 → **虚假绿灯**。

**修复建议:**
```python
# 验证 hook 调用的目标脚本本身包含 review 入口
assert "kanban_refresh" in content
# 然后单独测试 kanban_refresh.py 必须包含 local_codex_review 调用
# 见建议改进 #1
```

---

### 2. 关键脚本未在 diff 中 - 评审盲区
**文件:** `.git/hooks/post-commit` 和 `scripts/kanban_refresh.py`(推测路径)

**问题:** 本次 diff 改了 2 个测试文件,但 **post-commit hook 的实际内容 和 `kanban_refresh` 脚本本身没出现在 diff 里**。评审只能验证 hook 文件包含 `kanban_refresh` 字符串,无法验证:
- `kanban_refresh` 是否真的集成了 `local_codex_review`?
- `local_codex_review` 是否仍符合"不调 codex CLI"原则?
- 4 步骤 + cron + post-commit 的完整链路是否安全?

**修复建议:**
1. L4.12 commit 必须把 `kanban_refresh.py`、`post-commit` 完整内容加入 diff
2. 或在 PR 描述里贴出 hook 全文 + `kanban_refresh.py` 关键调用链

---

### 3. `test_local_codex_review.py` 测试语义反转
**文件:** `03-执行/tests/unit/test_local_codex_review.py:49-55`

```python
# 原版(模糊但意图明确:必须用 local_codex_review):
assert "local_codex_review" in content
assert "codex review" not in content or "codex review" in content and "scripts/codex_review.py" not in content

# 新版(过度严格 + 命名误导):
assert "kanban_refresh" in content
assert "codex review" not in content
```

**问题 A — 测试名与内容不符:** 函数名仍是 `test_post_commit_uses_local_review`,但实际测的是 `kanban_refresh`。**测试名撒谎** → 违反 v42。

**问题 B — 反向断言过严:** `assert "codex review" not in content` 意味着 hook 文件中**任何位置**都不能出现 "codex review" 字符串(包括注释、shebang、log 文本)。一行 `# TODO: 不要再调 codex review` 都会让测试红。这不防"调 CLI",只防"字符串出现"。

**修复建议:**
```python
def test_post_commit_uses_kanban_refresh():  # 改名!
    """post-commit hook 必须用 kanban_refresh(L4.12 集成入口)"""
    content = (PROJECT_ROOT / ".git/hooks/post-commit").read_text(encoding="utf-8")
    assert "kanban_refresh" in content
    # 用更精准的正则:不在注释里、且看起来像命令调用
    import re
    code_lines = [
        line for line in content.splitlines()
        if "codex review" in line and not line.strip().startswith("#")
    ]
    assert not code_lines, f"hook 中不应有非注释的 codex review 调用: {code_lines}"
```

---

## ⚠️ 中等问题(应该修复)

### 4. 看板 commit log 移除 `cbb0e99` - 显示策略需明确
**文件:** `.kanban/index.html:308`

**问题:** commit `cbb0e99 feat(state): 模式 v2.0 - STATE.json 单一真值源 + 自动生成` 从看板消失。这是一个**关键架构 commit**(STATE.json 单一真值源),被刷出可见列表意味着:
- 要么显示数量限制为 9(刚被挤掉)
- 要么有过滤逻辑误伤了它

**修复建议:**
1. 确认看板生成器的 `MAX_COMMITS` 常量是否够大(建议 ≥ 15)
2. 或在 STATE.json 里有 `pinned_commits` 字段,把架构级 commit 钉住

---

### 5. STATE.json 时间戳跨度异常
**文件:** `STATE.json:142`

`reviewed_at`: `12:04:59` → `12:05:50`(间隔 51 秒)。而 `
